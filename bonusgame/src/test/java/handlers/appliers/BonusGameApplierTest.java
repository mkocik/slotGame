package handlers.appliers;

import accounts.Player;
import bonus.inputHandler.InputHandlerFactory;
import org.junit.Test;

import static org.mockito.Matchers.any;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.times;
import static org.mockito.Mockito.verify;

/**
 * Created by maciej.kocik on 2/9/2017.
 */
public class BonusGameApplierTest {

    @Test
    public void shouldApplyWin() {
        /*InputHandlerFactory inputHandlerFactory = mock(InputHandlerFactory.class);
        Player player = mock(Player.class);

        BonusGameApplier bonusGameApplier = new BonusGameApplier(inputHandlerFactory, player);
        bonusGameApplier.applyWin();

        verify(inputHandlerFactory, times(1)).createAutoInputHandler(any(), any());*/
    }
}
